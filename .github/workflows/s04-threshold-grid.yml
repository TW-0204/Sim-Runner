name: S04 Threshold Grid

on:
  push:
    paths:
      - '.github/workflows/s04-threshold-grid.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  threshold:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        threshold: [4, 6, 8, 10]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24.x'
      - name: Run S04 threshold precision
        run: node --import ./scripts/register-ts-hooks.mjs scripts/s04-threshold-run.ts --threshold "${{ matrix.threshold }}" --games 1000
      - name: Upload result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: s04-threshold-${{ matrix.threshold }}
          path: s04-threshold-results/
          if-no-files-found: error
          retention-days: 7
