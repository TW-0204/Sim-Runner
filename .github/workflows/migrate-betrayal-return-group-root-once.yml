name: migrate betrayal return group root once

on:
  push:
    branches:
      - refactor/canonical-v11
    paths:
      - 'archive/migrations/refactor-betrayal-return-group-root-once.py'
      - '.github/workflows/migrate-betrayal-return-group-root-once.yml'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/canonical-v11
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24.x'
      - name: Rehome groups when finished Betrayal pieces return
        run: python archive/migrations/refactor-betrayal-return-group-root-once.py
      - name: Run canonical smoke
        run: bash scripts/run-s16-p10-smoke.sh
      - name: Run 30 canonical regressions
        run: node --import ./scripts/register-ts-hooks.mjs scripts/canonical-regression-seeds.ts
      - name: Commit Betrayal return lifecycle fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/lib/game/engine.ts
          git commit -m "fix: rehome group root when betrayal piece returns"
          git push origin HEAD:refactor/canonical-v11
