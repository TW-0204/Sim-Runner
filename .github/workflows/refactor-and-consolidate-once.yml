name: refactor and consolidate canonical source once

on:
  push:
    branches:
      - refactor/canonical-v11

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/canonical-v11
          fetch-depth: 0
      - name: Remove forced-precision source mutation
        run: python scripts/refactor-critical-force-once.py
      - name: Consolidate canonical source layout
        run: python scripts/consolidate-canonical-once.py
      - name: Commit structural consolidation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm -f .github/workflows/refactor-and-consolidate-once.yml
          git rm -f scripts/refactor-critical-force-once.py
          git rm -f scripts/consolidate-canonical-once.py
          git add -A
          git commit -m "refactor: retire patch stack and canonicalize source layout"
          git push origin HEAD:refactor/canonical-v11
